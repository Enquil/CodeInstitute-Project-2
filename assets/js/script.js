    // Set login to false on screen load 
document.addEventListener('DOMContentLoaded'), function() {
        
    document.getElementById("clicker").addEventListener("click", Hello)
    
        
}

// check username and pass against object

function Hello () {
    console.log(getElementById(uname).innerText);
}